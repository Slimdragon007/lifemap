# Copy to tests/e2e/.env.e2e (gitignored) to enable de-demo.spec.ts.
#
# Use a DEDICATED Supabase test user on the LifeMap project — NOT a real
# personal account. The user's row should be empty so the guard can assert that
# a real authenticated session renders the user's own state, never "Alex Kim".
E2E_EMAIL=e2e@getlifemap.com
E2E_PASSWORD=

node_modules/
dist/
.env
.env.local
.dev.vars
worker/.dev.vars
*.log
supabase/.temp/
.worktrees/

# Playwright E2E + visual regression
# (test-results/ + playwright-report/ cover both suites; the committed visual
# baselines under tests/visual/**/*.png are deliberately NOT ignored)
/test-results/
/playwright-report/
/playwright/.cache/
tests/e2e/.env.e2e
# Keep the committed demo-mode env (overrides the .env ignore above)
!tests/e2e/env-demo/.env

# scratch agent reports (never ship)
.merge-report.md
.task1-report.md
.task1-report*.md
.*-report.md

# wrangler local state
worker/.wrangler/

# Graphify generated cache; committed graph outputs live in graphify-out/
/graphify-out/cache/

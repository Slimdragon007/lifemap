# Server-side only (never exposed to the browser)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.5
SUPABASE_SERVICE_ROLE_KEY=

# Client-side (Vite exposes VITE_* to the browser — anon key is safe there, RLS enforces access)
VITE_API_ORIGIN=
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
